# Media

Drop images / gifs / short clips used by the project READMEs here (e.g. `hero.gif`, the testbed
photo, result figures), then reference them from the root [README](../../README.md) or the docs.

Suggested, lightweight set for the showcase:
- `hero.gif` — the robot driving the grid under the overhead camera (a few seconds).
- `tracker.png` — the tracker view with the AR grid overlay + energy map.
- `setup.jpg` — a photo of the physical testbed (robot + markers + camera).
- result figures (once the report's figure set is settled).

Keep files reasonably small (compress gifs/clips) so the repo stays light.

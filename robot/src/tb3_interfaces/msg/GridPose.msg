# A position and heading on a 2D grid.
# x, y are grid coordinates (0-indexed, sub-cell precision).
# heading is in radians, ROS convention (0 = +X axis, CCW positive).
float64 x
float64 y
float64 heading
